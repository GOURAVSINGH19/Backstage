# ${{ values.title }} (`${{ values.name }}`)

This repository contains the Backstage catalog registration manifest and cluster metadata for **${{ values.title }}**.

## Cluster Overview

- **Cluster Name**: `${{ values.name }}`
- **Provider**: `${{ values.provider }}`
- **Environment**: `${{ values.environment }}`
- **API Server Endpoint**: `${{ values.apiUrl }}`
- **Owner**: `${{ values.owner }}`

---
*Created via Backstage Scaffolder Cluster Template*
